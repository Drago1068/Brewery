# Shared Types

The versioned OpenAPI schema is the source for generated frontend types. Phase 1A keeps handwritten UI view models local until schema generation is added after the architecture gate.

